dojo.provide("phpr.Calendar.Tree");
dojo.require("phpr.Default.Tree");

dojo.declare("phpr.Calendar.Tree", phpr.Default.Tree, {
    // summary:
    //    This class is responsible for rendering the Tree of a Calendar module
    // description:
    //    The tree for the Calendar module is rendered -  at the moment it is exactly
    //    the same tree as the Default tree
	constructor: function() {
    }
});